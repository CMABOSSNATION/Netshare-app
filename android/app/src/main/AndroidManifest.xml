<?xml version="1.0" encoding="utf-8"?>
<!-- FIX: Removed package="com.netshare" attribute — AGP 8.x uses namespace in build.gradle instead.
     Having package= here causes a build error with modern Gradle. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- FIX: Added missing FOREGROUND_SERVICE_CONNECTED_DEVICE (required for Android 14+ VPN foreground service) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />

    <application
        android:name=".MainApplication"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:allowBackup="false"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- VPN Service - MUST have BIND_VPN_SERVICE permission -->
        <!-- FIX: Use fully-qualified class name so Android resolves it correctly
             regardless of the app's applicationId (com.netsharebuild in the build
             scaffold vs com.netshare in the source). Relative names like
             ".NetShareVpnService" resolve against applicationId, pointing to a
             class that doesn't exist, so Android silently ignores the service
             and VpnService.prepare() never shows the permission dialog. -->
        <service
            android:name="com.netshare.NetShareVpnService"
            android:permission="android.permission.BIND_VPN_SERVICE"
            android:exported="false"
            android:foregroundServiceType="connectedDevice">
            <intent-filter>
                <action android:name="android.net.VpnService" />
            </intent-filter>
        </service>

    </application>

</manifest>
