<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <!-- Required to get the device WiFi IP address -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- Required to keep tunnel WS alive when app is backgrounded -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- Prevent CPU sleep while tunnel is active -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!--
      VPN permission — lets NetShareVpnService create a TUN interface on the
      CLIENT device.  This is what intercepts ALL outbound packets and routes
      ONLY our proxy traffic (127.0.0.1:8899) to the internet; everything else
      (background app data, ads, trackers) is silently dropped.
      The user sees a single system "VPN connection request" dialog on first run.
    -->
    <uses-permission android:name="android.permission.BIND_VPN_SERVICE" />

    <application
        android:name=".MainApplication"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:allowBackup="false"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          NetShareVpnService — CLIENT-SIDE only.
          Creates a TUN interface that captures all outbound traffic.
          Routes only 127.0.0.1:8899 (our proxy) through; drops the rest.
          Must declare android:permission so only the system can bind it.
        -->
        <service
            android:name=".NetShareVpnService"
            android:permission="android.permission.BIND_VPN_SERVICE"
            android:exported="false">
            <intent-filter>
                <action android:name="android.net.VpnService" />
            </intent-filter>
        </service>

    </application>

</manifest>
